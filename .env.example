# Neon Database URLs
# Pooled connection for runtime queries
DATABASE_URL_POOLED=postgresql://user:pass@host/db?sslmode=require

# Direct connection for migrations and Drizzle Kit operations
DATABASE_URL_SINGLE=postgresql://user:pass@host/db?sslmode=require
