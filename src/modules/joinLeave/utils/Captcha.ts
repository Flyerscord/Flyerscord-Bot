import { EmbedBuilder, User } from "discord.js";

export async function sendCaptcha(user: User): Promise<void> {}

function getCaptchaEmbed(): EmbedBuilder {
  const embed = new EmbedBuilder();

  return embed;
}
