import { AuditLogEvent, Guild } from "discord.js";

// export async function getMostRecentEvent(guild: Guild, type: AuditLog) {
//   const logs = await guild.fetchAuditLogs();
//   return logs.entries.find((entry) => {
//     if (entry.actionType == type) {
//     }
//   });
// }
